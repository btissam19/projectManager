<!-- ... -->
<!DOCTYPE html>
<html lang="es">
<head>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link rel="stylesheet" href="/CSS/dashboardout.css">
    <style>
        @media (max-width: 768px) {
            .flex-wrap {
                display: flex;
                flex-wrap: wrap;
            }
            .section-small {
                width: 50%;
            }
        }
    </style>
</head>
 
<body class="bg-gray-100">
    <div class="min-h-screen flex items-center justify-center ">
        <form class="single-task-form bg-white shadow-lg rounded-lg p-8 px-16 space-y-6 max-w-md w-full">
            <h4 class="text-center text-2xl font-semibold text-gray-500 mb-6 mt-8">Edit Task</h4>
            
            <div class="form-control mb-6 ">
                <label class="block text-sm font-medium text-gray-700 mb-2">Task ID</label>
                <p class="task-edit-id w-full px-4 py-2 bg-gray-200 rounded  text-center">{{task._id}}</p>
            </div>
            
            <div class="form-control mb-6">
                <label for="name" class="block text-sm font-medium text-gray-700 mb-2">Name</label>
                <input type="text" name="name" class="task-edit-name w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-cyan-500" value="{{task.name}}" />
            </div>
            
            <div class="form-control flex items-center justify-center mb-6">
                <label for="completed" class="text-sm font-medium text-gray-700 mr-4">Completed</label>
                <input type="checkbox" name="completed" class="task-edit-completed" {{#if task.completed}}checked{{/if}} />
            </div>
            
            <div class="w-1/2 mx-auto mb-4">
                <button type="submit" class="bg-gradient-to-r from-cyan-400 to-cyan-600 text-white text-center py-2 rounded-lg w-full focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-cyan-500">Edit</button>
            </div>
            
            <div class="text-center">
                 <a href="/task" class="text-cyan-600">Back to tasks</a>
            </div>
            
            <div class="form-alert text-red-dark text-center mt-4"></div>
        </form>
    </div>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/axios/0.21.1/axios.min.js" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
    <script src="/JS/edittask.js"></script>
</body>

</html>

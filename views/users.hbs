<!DOCTYPE html>
<html lang="en">

<head>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link rel="stylesheet" href="/CSS/dashboardout.css">
    <style>
        @media (max-width: 768px) {
            .flex-wrap {
                display: flex;
                flex-wrap: wrap;
            }

            .section-small {
                width: 50%;
            }
        }
    </style>
</head>

<body class="bg-gray-100">

    <div class="min-w-screen min-h-screen bg-gray-100 flex items-center justify-center font-sans overflow-hidden"> <!-- Changed justify-cente to justify-center -->
        <div class="w-full lg:w-5/6">
            <div class="bg-white shadow-md rounded my-6">
                {{#if users}}
    <table class="min-w-max w-full table-auto bg-white shadow-md rounded-lg m-auto" id="tablDom">
        <thead>
            <tr class="bg-gradient-to-r from-cyan-400 to-cyan-600 text-white">
                <th class="py-3 px-6 text-left font-bold uppercase text-sm leading-normal w-1/2">Full Name </th>
                <th class="py-3 px-6 text-center font-bold uppercase text-sm leading-normal w-1/2">Username</th>
            </tr>
        </thead>
        <tbody class="text-gray-600 text-sm font-light divide-y divide-gray-200">
            {{#each users}}
                <tr class="hover:bg-gray-100 transition-all duration-200">
                    <td class="py-3 px-6 text-left">
                        <div class="flex items-center">
                            <span class="font-medium" data-email="{{username}}"></span>
                        </div>
                    </td>
                    <td class="py-3 px-6 text-center">
                        <span class="font-medium">{{username}}</span>
                    </td>
                </tr>
            {{/each}}
        </tbody>
    </table>
{{else}}
    <div class="flex items-center justify-center  text-3xl text-red-600 font-bold">
        No users found.
    </div>
{{/if}}

               
</div>
 <div class="text-center">
        <a href="/Admindashboard" class="text-cyan-600">back to dashboard</a></p></div>
            </div>
        </div>
    </div>
    <script src="/JS/users.js"></script>
</body>

</html>
